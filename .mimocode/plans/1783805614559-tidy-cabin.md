# Migrar Frontend de Vercel para Cloudflare Pages

## Contexto
O frontend da BibliotecAI é um SPA puro (React + Vite), sem SSR. Atualmente hospedado no Vercel com deploy automático via git. O projeto já usa Cloudflare Workers para o API Gateway. A migração unifica toda a infraestrutura no ecossistema Cloudflare.

## O que precisa mudar

### 1. Criar `_redirects` (SPA routing)
**Arquivo:** `public/_redirects`
```
/* /index.html 200
```
Substitui o rewrite do `vercel.json`.

### 2. Criar `_headers` (security headers)
**Arquivo:** `public/_headers`
```
/*
  X-Frame-Options: SAMEORIGIN
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
```
Substitui os headers do `vercel.json`.

### 3. Remover código Vercel-specific do frontend
- **`src/hooks/useTenant.jsx`**: Remover lógica de `VITE_VERCEL_PROJECT_HOST` e detecção `.vercel.app`. Cloudflare Pages suporta wildcard nativamente.
- **`src/pages/AdminTenants.jsx`**: Remover aviso sobre Vercel não suportar subdomínios wildcard.
- **`src/App.jsx`**: Remover função `isLegacyDisabledHost()` que checa `bibliotec-ai-core.vercel.app`.
- **`src/hooks/useAnalytics.js`**: Verificar se referencia Vercel.

### 4. Migrar cron job (opcional/deprecar)
O cron `api/disable-legacy-domain.js` é Vercel-specific (remove domínio legado `.vercel.app`). Opções:
- **Recomendado**: Deprecar — após migração o domínio legado não importa mais
- Alternativa: Criar Cloudflare Worker com Cron Trigger (desnecessário)

### 5. Criar Cloudflare Pages project
```bash
npx wrangler pages project create bibliotecai-frontend
```

### 6. Configurar variáveis de ambiente no Cloudflare Pages
Migrar todas as `VITE_*` do Vercel para o Cloudflare Pages dashboard:
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_PUBLISHABLE_KEY`
- `VITE_PLATFORM_API_BASE_URL`
- `VITE_BIBLIOTECA_AI_API_URL`
- `VITE_APP_BASE_DOMAIN`
- `VITE_APPWRITE_ENDPOINT`
- `VITE_APPWRITE_PROJECT_ID`
- `VITE_APPWRITE_DATABASE_ID`
- `VITE_ENABLE_OPTIONAL_STUDENT_FEATURES`
- `VITE_ENABLE_NATIVE_PUSH`
- `VITE_APP_ENV`
- `VITE_APP_ENV_LABEL`
- Remover `VITE_VERCEL_PROJECT_HOST`

### 7. Deploy via Wrangler
Build command: `npm run build:web`
Output directory: `dist`

### 8. Configurar domínio customizado
- Apontar DNS `bibliotecai.com.br` e `*.bibliotecai.com.br` para Cloudflare Pages
- Como o DNS já está no Cloudflare, basta criar registros CNAME no painel
- Cloudflare Pages suporta wildcard nativamente

### 9. Remover Vercel
- Desconectar repositório do Vercel
- Deletar projeto no Vercel (após confirmar Cloudflare Pages funcionando)
- Remover `vercel.json` e `api/` directory

## Arquivos a modificar
- `public/_redirects` (criar)
- `public/_headers` (criar)
- `src/hooks/useTenant.jsx` (remover lógica Vercel)
- `src/pages/AdminTenants.jsx` (remover aviso Vercel)
- `src/App.jsx` (remover isLegacyDisabledHost)
- `vercel.json` (remover)
- `api/disable-legacy-domain.js` (remover)

## Arquivos NÃO modificar
- `vite.config.js` (build config já é genérico)
- `package.json` (scripts já existem: `build:web`)
- `capacitor.config.json` (independente de hosting)
- `cloudflare/api-gateway/` (worker separado, não afeta)

## Ordem de execução
1. Criar `public/_redirects` e `public/_headers`
2. Limpar código Vercel-specific do frontend
3. Criar projeto no Cloudflare Pages + configurar env vars
4. Deploy inicial via `npx wrangler pages deploy dist`
5. Configurar domínio customizado no painel Cloudflare
6. Testar tudo funcionando
7. Remover `vercel.json` e `api/`
8. Desconectar/deletar Vercel

## Verificação
1. `npm run build:web` — build limpo
2. `npx wrangler pages deploy dist` — deploy sem erros
3. Acessar `bibliotecai.com.br` — site carrega
4. Testar rotas internas (SPA routing funciona)
5. Testar login/logout
6. Testar multi-tenant (subdomínio de escola)
7. Verificar security headers no browser
8. `npm test` — testes passam
